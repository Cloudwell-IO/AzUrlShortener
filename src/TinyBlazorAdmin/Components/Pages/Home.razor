@page "/"

<PageTitle>Cloudwell Go - Admin Center</PageTitle>

<div class="cwgo-container">
                <h1 class="cwgo-hero-title">Cloudwell Go - Admin Center</h1>

    <!-- HERO -->
    <FluentCard Class="cwgo-hero-card">
        <FluentStack Horizontal="true" Gap="16" VerticalAlignment="VerticalAlignment.Center">
            <div class="cwgo-hero-icon">
                <span>Go</span>
            </div>
            <FluentStack Gap="8" VerticalAlignment="VerticalAlignment.Center">
                <p class="cwgo-hero-text">
                    Cloudwell Go is Cloudwell’s internal vanity URL service for creating short,
                    memorable links to events, documentation, product pages, marketing campaigns,
                    and other work-related resources.
                </p>
                <p class="cwgo-hero-subtext">
                    Use this portal to create, update, and manage short links under
                    <b>go.cloudwell.io</b>, such as
                    <code>go.cloudwell.io/demo</code> or
                    <code>go.cloudwell.io/getcalendaroverlay</code>.
                </p>
            </FluentStack>
        </FluentStack>
    </FluentCard>

    <!-- LOWER GRID -->
    <div class="cwgo-lower-grid">

        <FluentCard Class="cwgo-info-card">
            <h2 class="cwgo-section-title">Usage guidelines</h2>
            <p>
                Cloudwell Go is provided for <b>Cloudwell business purposes only</b>.
                When creating or updating links, please:
            </p>
            <ul class="cwgo-list">
                <li>Use links only for professional, work-related content.</li>
                <li>Do not create links that point to inappropriate, offensive, or non-business sites.</li>
                <li>Follow all Cloudwell security, privacy, and information-handling policies.</li>
                <li>
                    Choose clear, descriptive slugs that reflect the destination
                    (for example, <code>/m365dc</code>, <code>/overlay-demo</code>).
                </li>
            </ul>
        </FluentCard>

        <FluentCard Class="cwgo-info-card cwgo-disclaimer-card">
            <h2 class="cwgo-section-title">Disclaimer</h2>
            <p>
                Cloudwell may review, update, or remove Cloudwell Go links at any time to maintain
                accuracy, security, and compliance. Links and their destinations may be modified,
                redirected, or disabled <b>without prior notice</b>.
            </p>
            <p>
                By using this tool, you agree to manage links responsibly and in accordance with
                Cloudwell policies and guidelines, and to use Cloudwell Go only for professional purposes.
            </p>
            <p class="cwgo-note">
                Note: If a link is adjusted or retired, the owning team is responsible for communicating
                any changes to their audiences.
            </p>
        </FluentCard>

    </div>
</div>

<style>
    .cwgo-container {
        max-width: 1280px;
        margin: 0 auto;
        padding: 16px 24px 32px;
        box-sizing: border-box;
    }

    .cwgo-hero-card {
        margin-bottom: 20px;
        padding: 20px 24px;
        border-left: 4px solid #0b5475;
        background-color: var(--neutral-layer-1);
    }

    .cwgo-hero-icon {
        width: 56px;
        height: 56px;
        border-radius: 50%;
        background-color: #0b5475;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #ffffff;
        font-weight: 700;
        font-size: 1.1rem;
        text-transform: uppercase;
    }

    .cwgo-hero-title {
        margin: 0;
        font-size: 1.6rem;
        font-weight: 600;
        color: var(--neutral-foreground-rest);
    }

    .cwgo-hero-text {
        margin: 4px 0;
        font-size: 0.98rem;
        line-height: 1.6;
    }

    .cwgo-hero-subtext {
        margin: 0 0 8px;
        font-size: 0.92rem;
        line-height: 1.5;
        color: var(--neutral-foreground-hint, #555);
    }

    .cwgo-lower-grid {
        display: grid;
        grid-template-columns: minmax(0, 1.4fr) minmax(0, 1.1fr);
        gap: 16px;
    }

    .cwgo-info-card {
        padding: 16px 20px;
        background-color: var(--neutral-layer-1);
    }

    .cwgo-section-title {
        margin-top: 0;
        margin-bottom: 6px;
        font-size: 1.1rem;
        font-weight: 600;
        color: var(--neutral-foreground-rest);
    }

    .cwgo-list {
        margin: 6px 0 0;
        padding-left: 1.25rem;
        font-size: 0.95rem;
        line-height: 1.6;
    }

    .cwgo-disclaimer-card {
        border-left: 3px solid var(--accent-fill-rest);
    }

    .cwgo-note {
        margin-top: 8px;
        font-size: 0.9rem;
        color: var(--neutral-foreground-hint, #666);
    }

    code {
        font-family: Consolas, "SF Mono", Menlo, Monaco, "Courier New", monospace;
        padding: 0.05rem 0.3rem;
        border-radius: 3px;
        background-color: var(--neutral-layer-2);
        font-size: 0.85em;
    }

    @@media (max-width: 960px) {
        .cwgo-lower-grid {
            grid-template-columns: minmax(0, 1fr);
        }

        .cwgo-container {
            padding-inline: 16px;
        }
    }
</style>
